version: 2.1
jobs:
  build:
     docker:
       - image: circleci/openjdk:8-jdk
       - image: circleci/mysql:5.7.30
         environment:
         - MYSQL_ROOT_PASSWORD: sapnaneu
         - MYSQL_DATABASE: Assignment
     steps:
       - checkout
       - run:
           name: Build WAR file
           command:  |
             echo 'test'
             pwd
             ls -al
             cd assignment
             mvn clean install
             zip –r webapp.zip webapp
       - aws-s3/copy:
          arguments: '--dry-run'
          from: assignment/webapp.zip
          to: 's3://codedeploye.sapnapatel.me'
orbs:
  aws-s3: circleci/aws-s3@1.0.15  